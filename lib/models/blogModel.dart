class BlogModel {
  String title, desc, blogUrl, imageurl, published, content;

  BlogModel(
      {this.title,
      this.desc,
      this.blogUrl,
      this.imageurl,
      this.published,
      this.content});
}
